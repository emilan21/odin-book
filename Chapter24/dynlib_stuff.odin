package dynlib_stuff

// IF you have a .dll, .dylib or .so file and you want to load symbols from it,
// so that you for example can run procedures inside that library, then
// you can use the core:dynlib package
import "core:dynlib"

main :: proc() {
}
